# 0001 — New crate/binary identity

- **Finding**: research.md F1/F2 (rename is required; naming convention
  space)
- **Question**: `agent-spec` is confirmed taken by upstream's own active
  crate (crates.io policy rules out a transfer). What replaces it?
- **Teaching**: walked through crates.io's usage policy (first-come,
  first-served; squatting-reclaim doesn't apply to an actively published
  crate), the sibling-fork precedent (`BUNotesAI/specwright` solved the
  same collision with a `-wright` craftsman suffix), and the blast radius
  beyond Cargo.toml (binary name, embedded skill strings, README,
  repository/homepage already wrong).
- **User's redirect**: the initial candidate set leaned on
  "spec"/"contract" vocabulary. User reframed: "主要我们是做文档，
  learning，不一定需要spec" — the differentiating value built this
  session (Doc Impact Guard, single docs/ household, Research and Learn,
  Learning Archive, capabilities library) is documentation governance and
  decision learning, not spec-checking. Re-searched availability for
  documentation/learning-oriented `-wright` compounds
  (docwright/docsteward/learnwright/knowledgewright — chronicle,
  compendium, steward, canon, archivist, curator were all already taken).
- **User's answer**: `docwright`.
- **Overrides**: the spec/contract-anchored candidate set from the first
  round (contractwright, taskcontract, etc.).
